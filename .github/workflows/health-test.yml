name: "Health tests (Built-In-Self-Test)"

on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Force-fail
        run: exit 1

      - uses: actions/checkout@v2
